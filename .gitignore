# Never commit real secrets / local env
.env
.env.local
*.local

# OS / editor / local tooling noise
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/

# Build artifacts & dependencies (rebuilt in the Docker image / CI)
node_modules/
voices/dist/
